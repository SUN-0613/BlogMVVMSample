<Window x:Class="BlogMVVMSample.Forms.View.TextBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BlogMVVMSample.Forms.ViewModel"
        mc:Ignorable="d"
        Title="TextBoxView" Height="250" Width="400">
    <Window.DataContext>
        <vm:TextBoxViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding Text}" />
        
        <!-- コードビハインドでクリック処理
            <Button Grid.Row="1" Content="Input Text" Click="Button_Click" />
        -->
        
        <!-- ViewModelでクリック処理 -->
        <Button Grid.Row="1" Content="Input Text">
            <Button.InputBindings>
                <MouseBinding Command="{Binding ButtonClickCommand}" MouseAction="LeftClick" />
            </Button.InputBindings>
        </Button>
        
    </Grid>
</Window>
