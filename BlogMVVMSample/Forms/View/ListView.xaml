<Window x:Class="BlogMVVMSample.Forms.View.ListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BlogMVVMSample.Forms.ViewModel"
        xmlns:data="clr-namespace:BlogMVVMSample.Data"
        xmlns:custom="clr-namespace:BlogMVVMSample.Custom"
        mc:Ignorable="d"
        Title="ListView" Height="450" Width="800">
    <Window.DataContext>
        <vm:ListViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        
        <custom:CustomTreeView Grid.Row="0" ItemsSource="{Binding Paths}" CustomSelectedItem="{Binding SelectedPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="data:PathInfo" ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </custom:CustomTreeView>

        <TextBlock Grid.Row="1" Text="{Binding FullPath}" VerticalAlignment="Center" />

    </Grid>
</Window>
